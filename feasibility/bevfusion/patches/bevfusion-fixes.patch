diff --git a/mmdet3d/models/backbones/radar_encoder.py b/mmdet3d/models/backbones/radar_encoder.py
index 67957e1..4436a05 100644
--- a/mmdet3d/models/backbones/radar_encoder.py
+++ b/mmdet3d/models/backbones/radar_encoder.py
@@ -15,7 +15,10 @@ from torchvision.utils import save_image
 from mmdet3d.ops import feature_decorator
 from mmcv.cnn.bricks.non_local import NonLocal2d
 
-from flash_attn.flash_attention import FlashMHA
+try:
+    from flash_attn.flash_attention import FlashMHA
+except ImportError:  # optional: only needed by the radar encoder
+    FlashMHA = None
 
 
 __all__ = ["RadarFeatureNet", "RadarEncoder"]
diff --git a/mmdet3d/models/vtransforms/depth_lss.py b/mmdet3d/models/vtransforms/depth_lss.py
index b7cd45d..a7cdc2e 100644
--- a/mmdet3d/models/vtransforms/depth_lss.py
+++ b/mmdet3d/models/vtransforms/depth_lss.py
@@ -24,7 +24,10 @@ class DepthLSSTransform(BaseDepthTransform):
         zbound: Tuple[float, float, float],
         dbound: Tuple[float, float, float],
         downsample: int = 1,
+        **kwargs,
     ) -> None:
+        # Forward newer BaseDepthTransform options (depth_input,
+        # add_depth_features, ...) that this subclass predates.
         super().__init__(
             in_channels=in_channels,
             out_channels=out_channels,
@@ -34,6 +37,7 @@ class DepthLSSTransform(BaseDepthTransform):
             ybound=ybound,
             zbound=zbound,
             dbound=dbound,
+            **kwargs,
         )
         self.dtransform = nn.Sequential(
             nn.Conv2d(1, 8, 1),
@@ -79,7 +83,7 @@ class DepthLSSTransform(BaseDepthTransform):
             self.downsample = nn.Identity()
 
     @force_fp32()
-    def get_cam_feats(self, x, d):
+    def get_cam_feats(self, x, d, mats_dict=None):  # mats_dict unused here
         B, N, C, fH, fW = x.shape
 
         d = d.view(B * N, *d.shape[2:])
diff --git a/mmdet3d/ops/feature_decorator/src/feature_decorator.cpp b/mmdet3d/ops/feature_decorator/src/feature_decorator.cpp
index 0999bb1..a61fe4a 100644
--- a/mmdet3d/ops/feature_decorator/src/feature_decorator.cpp
+++ b/mmdet3d/ops/feature_decorator/src/feature_decorator.cpp
@@ -11,7 +11,7 @@ at::Tensor feature_decorator_forward(
   const at::Tensor _y, 
   const at::Tensor _z, 
   const double vx, const double vy, const double x_offset, const double y_offset, 
-  int normalize_coords, int use_cluster, int use_center
+  int64_t normalize_coords, int64_t use_cluster, int64_t use_center
 ) {
   int n = _x.size(0);
   int c = _x.size(1);
diff --git a/setup.py b/setup.py
index 256e422..d3ac120 100644
--- a/setup.py
+++ b/setup.py
@@ -84,6 +84,12 @@ if __name__ == "__main__":
                 ],
                 extra_args=["-w", "-std=c++17"],
             ),
+            make_cuda_ext(
+                name="feature_decorator_ext",
+                module="mmdet3d.ops.feature_decorator",
+                sources=["src/feature_decorator.cpp"],
+                sources_cuda=["src/feature_decorator_cuda.cu"],
+            ),
             make_cuda_ext(
                 name="bev_pool_ext",
                 module="mmdet3d.ops.bev_pool",
